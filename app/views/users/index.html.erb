<%= link_to "Make A Reservation", new_reservation_path, :class => "btn btn-green"  %>
<div class="container">

<h1>View ALL Users</h1> <div>
</div>

<% @users.each do |user| %>
<ul>
  <li>
    <%= link_to user.name, user %>
  </li>
</ul>
<% end %>

<div>
  <%= link_to "Create New User", new_user_path, :class => "btn btn-primary"  %>
</div>

<h3><%= link_to "Create New User", new_user_path %></h3>
</div>
